# macOS-DeviceBattery

A simple CLI tool that reads the battery level of a macOS device.

## How to use

1. Run `./build.sh` to build the tool.

2. Run `./DeviceBattery <HID|SP> <device-name> [-1|0|1]`.

For `SP`, use `-1` for left, `0` for case, or `1` for right.
